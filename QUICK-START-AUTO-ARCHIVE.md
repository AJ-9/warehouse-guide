# 🚀 Быстрый старт: Автоматическое архивирование

## ✅ Уже настроено!

Автоматическое создание архива при каждом изменении **уже настроено и готово к работе**.

## 📝 Что нужно сделать:

### Шаг 1: Установите расширение "Run on Save"

1. Откройте Cursor/VS Code
2. Нажмите `Cmd+Shift+X` (или `Ctrl+Shift+X` на Windows/Linux)
3. Найдите расширение **"Run on Save"** от **emeraldwalk**
4. Нажмите **"Install"**

### Шаг 2: Перезагрузите редактор

После установки расширения перезагрузите Cursor/VS Code (`Cmd+Q` и откройте снова).

### Шаг 3: Готово! 🎉

Теперь при каждом сохранении файлов `.js`, `.html`, `.css`, `.json` будет автоматически создаваться архив!

## 🔍 Как проверить, что работает:

1. Откройте файл `app.js`
2. Добавьте пробел или комментарий (любое изменение)
3. Сохраните файл (`Cmd+S`)
4. Проверьте, что архив создан:
   ```bash
   ls -lt ~/Desktop/warehouse-guide-*.zip | head -1
   ```
5. Или проверьте лог:
   ```bash
   tail -1 archive-log.txt
   ```

## 📁 Где находятся архивы:

Все архивы сохраняются на **Desktop**:
```
/Users/alikbidzhiev/Desktop/warehouse-guide-*.zip
```

## 📝 История архивов:

Все созданные архивы записываются в файл:
```
archive-log.txt
```

## ⚙️ Если автоматическое архивирование не работает:

### Вариант 1: Проверьте расширение

Убедитесь, что расширение "Run on Save" установлено и активно.

### Вариант 2: Используйте ручное архивирование

```bash
./quick-archive.sh "описание изменений"
```

### Вариант 3: Используйте Node.js скрипт

```bash
node auto-archive-simple.js "описание изменений"
```

## 💡 Полезные команды:

```bash
# Просмотр последних архивов
ls -lt ~/Desktop/warehouse-guide-*.zip | head -5

# Просмотр лога архивов
cat archive-log.txt

# Отслеживание лога в реальном времени
tail -f archive-log.txt
```

---

**Статус:** ✅ Готово к использованию  
**Дата настройки:** 2025-01-27

