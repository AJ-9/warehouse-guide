# 📦 Автоматическое создание архивов

Система автоматического создания архивов проекта после каждого изменения.

## 🚀 Быстрое создание архива

```bash
# Создать архив с описанием изменений
./quick-archive.sh "описание изменений"

# Примеры:
./quick-archive.sh "bug-fix"
./quick-archive.sh "new-feature"
./quick-archive.sh "ui-update"
```

## 📋 Подробное создание архива

```bash
# Полный скрипт с детальной информацией
./create-archive.sh "описание изменений"
```

## 🔧 Node.js версия

```bash
# Создать архив
node auto-archive.js "описание изменений"

# Показать историю архивов
node auto-archive.js --log
```

## 📁 Структура архивов

Архивы создаются в папке `/Users/alikbidzhiev/Desktop/` с именами:
- `warehouse-guide-[описание]-[YYYYMMDD-HHMM].zip`

## 📝 Лог изменений

Все созданные архивы записываются в файл `archive-log.txt` с информацией:
- Дата и время создания
- Описание изменений
- Имя архива
- Размер архива

## ⚡ Автоматизация

Для автоматического создания архива после каждого изменения:

1. **После редактирования файлов** - запустите:
   ```bash
   ./quick-archive.sh "file-updated"
   ```

2. **После добавления новых функций** - запустите:
   ```bash
   ./quick-archive.sh "new-feature"
   ```

3. **После исправления ошибок** - запустите:
   ```bash
   ./quick-archive.sh "bug-fix"
   ```

## 📊 Мониторинг

Проверить историю созданных архивов:
```bash
node auto-archive.js --log
```

## 🎯 Рекомендации

- Используйте `quick-archive.sh` для быстрого создания
- Используйте `create-archive.sh` для детальной информации
- Всегда указывайте описательное название изменений
- Регулярно проверяйте лог архивов

---
*Система настроена для автоматического создания архивов после каждого изменения в проекте*
