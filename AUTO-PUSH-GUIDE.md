# 🚀 Руководство по автоматической отправке на GitHub

## Варианты автоматизации

### Вариант 1: Быстрая отправка одной командой (Рекомендуется)

**Использование:**
```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
./auto-push.sh "Описание изменений"
```

**Или без описания (автоматическое):**
```bash
./auto-push.sh
```

**Что делает:**
- ✅ Автоматически добавляет все изменения (`git add .`)
- ✅ Создает коммит с указанным сообщением
- ✅ Отправляет на GitHub (`git push origin main`)

---

### Вариант 2: Автоматическая отправка после каждого коммита

**Настроено автоматически!** После каждого `git commit` изменения автоматически отправляются на GitHub.

**Использование:**
```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
git add .
git commit -m "Ваше сообщение"
# Автоматически выполнится: git push origin main
```

---

### Вариант 3: Фоновый мониторинг изменений (Автоматическая отправка при сохранении файлов)

**Запуск:**
```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
./auto-push-on-change.sh
```

**Остановка:**
```bash
./stop-auto-push.sh
```

**Что делает:**
- 🔍 Отслеживает изменения файлов каждые 5 секунд
- ⏱️ Отправляет изменения на GitHub через 30 секунд после последнего изменения
- 📝 Ведет лог в файле `auto-push.log`
- 🔄 Работает в фоновом режиме

---

## Рекомендуемый способ работы

### Для разовых обновлений:
```bash
./auto-push.sh "Добавлен новый раздел для учетчика"
```

### Для постоянной работы:
1. Запустите фоновый мониторинг:
   ```bash
   ./auto-push-on-change.sh &
   ```
2. Работайте как обычно - изменения будут отправляться автоматически
3. При необходимости остановите:
   ```bash
   ./stop-auto-push.sh
   ```

---

## Алиасы для удобства (опционально)

Добавьте в `~/.zshrc` или `~/.bashrc`:

```bash
alias push-warehouse='cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2" && ./auto-push.sh'
alias start-auto-push='cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2" && ./auto-push-on-change.sh &'
alias stop-auto-push='cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2" && ./stop-auto-push.sh'
```

После этого можно использовать:
```bash
push-warehouse "Описание изменений"
start-auto-push
stop-auto-push
```

---

## Проверка статуса

```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
git status
git log --oneline -5
```

---

## Логи

- **auto-push.log** - лог автоматической отправки (при использовании фонового мониторинга)
- **.auto-push.lock** - файл блокировки (создается при запуске фонового процесса)

