# Инструкция по архивации проекта

## 📦 Ручная архивация после подтверждения изменений

Архивация теперь запускается **только после вашего подтверждения** изменений.

### Как создать архив:

1. **Простой способ** (с описанием по умолчанию):
   ```bash
   ./create-archive.sh
   ```

2. **С указанием описания изменений**:
   ```bash
   ./create-archive.sh "исправление нумерации глав"
   ./create-archive.sh "обновление контента главы 9"
   ./create-archive.sh "исправление ошибок"
   ```

### Примеры использования:

```bash
# После исправления нумерации
./create-archive.sh "исправление нумерации глав и подразделов"

# После обновления контента
./create-archive.sh "обновление раздела 9.1"

# После исправления ошибок
./create-archive.sh "исправление ошибок в app.js"

# Стандартная архивация
./create-archive.sh
```

### Где сохраняются архивы:

- **Расположение**: `/Users/alikbidzhiev/Desktop/`
- **Формат имени**: `warehouse-guide-[описание]-[дата-время].zip`
- **Пример**: `warehouse-guide-confirmed-update-20250115-143022.zip`

### Лог архивации:

Все созданные архивы записываются в файл `archive-log.txt` с указанием:
- Дата и время создания
- Описание изменений
- Имя архива
- Размер архива

### ⚠️ Важно:

- Автоматическая архивация **отключена**
- Архивация выполняется **только по вашему запросу**
- Каждый архив содержит полную копию проекта
- Старые архивы не удаляются автоматически

