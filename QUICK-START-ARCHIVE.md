# ⚡ Быстрый старт: Автоматическое архивирование

## 🚀 Запуск автоматического архивирования

### Вариант 1: Фоновый мониторинг (Рекомендуется)

Откройте терминал и запустите:

```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
./watch-and-archive.sh
```

Скрипт будет:
- ✅ Отслеживать все изменения файлов
- ✅ Автоматически создавать архив каждые 30 секунд (если были изменения)
- ✅ Сохранять архивы на Desktop
- ✅ Логировать все действия

**Остановка:** Нажмите `Ctrl+C` в терминале

### Вариант 2: Ручное создание архива

После каждого изменения просто запустите:

```bash
./quick-archive.sh "описание изменений"
```

Примеры:
```bash
./quick-archive.sh "bug-fix"
./quick-archive.sh "new-feature"
./quick-archive.sh "ui-update"
```

## 📁 Где находятся архивы?

Все архивы сохраняются в:
```
/Users/alikbidzhiev/Desktop/
```

Формат имени: `warehouse-guide-[описание]-[дата-время].zip`

## 📝 Просмотр истории

```bash
cat archive-log.txt
```

## 🔧 Установка fswatch (если нужно)

Для лучшей работы мониторинга:

```bash
brew install fswatch
```

## 💡 Совет

Для постоянной работы автоматического архивирования:
1. Запустите `./watch-and-archive.sh` в отдельном окне терминала
2. Оставьте его работать во время разработки
3. Все изменения будут автоматически архивироваться

---

**Готово!** Теперь каждый раз при изменении файлов будет создаваться архив проекта.

