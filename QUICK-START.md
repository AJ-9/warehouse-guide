# ⚡ Быстрый старт - Автоматическая отправка на GitHub

## 🎯 Самый простой способ (1 команда)

### Настройка (один раз):

```bash
echo 'alias wpush="cd /Users/alikbidzhiev/Desktop/warehouse-guide-corrected\ 2 && ./push"' >> ~/.zshrc
source ~/.zshrc
```

### Использование (из любой директории):

```bash
wpush "Описание изменений"
```

Или без описания:
```bash
wpush
```

---

## 🚀 Варианты использования

### 1. Быстрая отправка (рекомендуется)
```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
./push "Ваше описание"
```

### 2. Автоматическая отправка после коммита
**Уже настроено!** Просто делайте коммиты как обычно:
```bash
git add .
git commit -m "Ваше сообщение"
# Автоматически отправится на GitHub
```

### 3. Фоновый мониторинг (автоотправка при сохранении)
```bash
./auto-push-on-change.sh &
# Работайте как обычно, изменения отправятся автоматически
```

---

## 📋 Что уже настроено:

✅ Git репозиторий подключен к GitHub  
✅ Автоматическая отправка после каждого коммита (Git hook)  
✅ Скрипты для быстрой отправки  
✅ VS Code tasks для отправки через меню  

---

## 🔍 Проверка статуса

```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
git status
```

---

**Готово! Теперь вам не нужно вручную выполнять команды git add, commit и push.** 🎉

