# 📧 Правильная настройка шаблона EmailJS

## ⚠️ Важно: Использование переменных в шаблоне

В шаблоне EmailJS **ОБЯЗАТЕЛЬНО** должны использоваться переменные `{{message}}`, `{{from_name}}`, `{{from_email}}`, иначе вы будете получать только текст шаблона без данных пользователя.

## Настройка основного шаблона (для получения отзывов)

### Шаблон ID: `template_e9wgoaj`

1. Войдите на [EmailJS.com](https://www.emailjs.com/)
2. Перейдите в **"Email Templates"**
3. Откройте шаблон `template_e9wgoaj`
4. Настройте следующим образом:

**Subject (Тема письма):**
```
Отзыв от {{from_name}} - Справочник СКЛАД
```

**Content (Содержимое письма) - HTML формат:**
```html
<h2>Новый отзыв из приложения "Справочник СКЛАД"</h2>

<p><strong>Имя отправителя:</strong> {{from_name}}</p>
<p><strong>Email отправителя:</strong> {{from_email}}</p>

<hr>

<h3>Сообщение:</h3>
<p style="background-color: #f5f5f5; padding: 15px; border-radius: 5px; white-space: pre-wrap;">{{message}}</p>

<hr>

<p style="color: #666; font-size: 12px;">Это письмо отправлено автоматически из приложения "Справочник СКЛАД"</p>
```

**Или в текстовом формате:**
```
Новый отзыв из приложения "Справочник СКЛАД"

Имя отправителя: {{from_name}}
Email отправителя: {{from_email}}

---
Сообщение:
{{message}}
---

Это письмо отправлено автоматически из приложения "Справочник СКЛАД"
```

**To Email (Адрес получателя):**
```
aokha.aj@gmail.com
```

**From Name (Имя отправителя):**
```
Справочник СКЛАД
```

**Reply To (Ответить на):**
```
{{from_email}}
```

## Создание шаблона для автоответа

### Шаг 1: Создайте новый шаблон для автоответа

1. В разделе **"Email Templates"** нажмите **"Create New Template"**
2. Назовите его "Автоответ на отзыв"
3. Сохраните и получите новый **Template ID** (запишите его)

### Шаг 2: Настройте шаблон автоответа

**Subject (Тема):**
```
Спасибо за ваш отзыв! - Справочник СКЛАД
```

**Content (Содержимое):**
```html
<h2>Спасибо за ваш отзыв!</h2>

<p>Здравствуйте, {{from_name}}!</p>

<p>Мы получили ваше сообщение:</p>
<p style="background-color: #f5f5f5; padding: 15px; border-radius: 5px; white-space: pre-wrap;">{{message}}</p>

<p>Мы обязательно рассмотрим ваш отзыв и учтем ваши предложения при дальнейшем развитии приложения.</p>

<p>С уважением,<br>
Команда "Справочник СКЛАД"</p>

<hr>
<p style="color: #666; font-size: 12px;">Это автоматический ответ. Пожалуйста, не отвечайте на это письмо.</p>
```

**To Email (Адрес получателя):**
```
{{from_email}}
```

**From Name:**
```
Справочник СКЛАД
```

**Reply To:**
```
aokha.aj@gmail.com
```

### Шаг 3: Обновите код

После создания шаблона автоответа, обновите код в `app.js`:
- Найдите константу `EMAILJS_CONFIG`
- Добавьте `autoReplyTemplateId: 'ваш_template_id_автоответа'`

## Проверка переменных

Убедитесь, что в шаблоне используются правильные имена переменных:
- `{{from_name}}` - имя отправителя
- `{{from_email}}` - email отправителя  
- `{{message}}` - текст сообщения
- `{{to_email}}` - адрес получателя (если используется)

## Тестирование

1. Сохраните изменения в шаблоне
2. Отправьте тестовое сообщение через форму
3. Проверьте, что в письме отображаются:
   - ✅ Имя отправителя
   - ✅ Email отправителя
   - ✅ Текст сообщения (самое важное!)

---

**Важно:** Если вы видите только текст шаблона без данных, значит переменные не используются или используются неправильно!

