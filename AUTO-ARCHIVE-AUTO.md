# 🤖 Автоматическое архивирование - Полная автоматизация

## ✅ Решение реализовано!

Создан автоматический демон, который работает в фоне и создает архивы при каждом изменении файлов.

## 🚀 Быстрый старт

### Запуск автоматического архивирования:

```bash
./start-auto-archive.sh
```

**Готово!** Теперь при каждом изменении файлов `.js`, `.html`, `.json` будет автоматически создаваться архив.

### Остановка:

```bash
./stop-auto-archive.sh
```

## 📋 Как это работает

1. **Демон запускается в фоне** и отслеживает изменения файлов:
   - `app.js`
   - `index.html`
   - `sw.js`
   - `manifest.json`

2. **При обнаружении изменений** автоматически создается архив с задержкой 10 секунд (debounce)

3. **Архивы сохраняются** на Desktop в формате:
   ```
   warehouse-guide-auto-change-YYYYMMDD-HHMMSS.zip
   ```

4. **Все действия логируются** в `archive-log.txt`

## 🔍 Проверка статуса

```bash
# Проверить, запущен ли демон
ps aux | grep auto-archive-daemon

# Или проверить PID файл
cat .archive-daemon.pid

# Просмотреть логи
tail -f archive-daemon.log
```

## ⚙️ Настройки

### Изменить интервал между архивами

Отредактируйте `auto-archive-daemon.js`:

```javascript
this.debounceSeconds = 10; // Измените на нужное значение (в секундах)
```

### Добавить отслеживание других файлов

Отредактируйте `auto-archive-daemon.js`:

```javascript
this.watchedFiles = [
    path.join(this.projectDir, 'app.js'),
    path.join(this.projectDir, 'index.html'),
    // Добавьте свои файлы здесь
];
```

## 🔧 Устранение неполадок

### Демон не запускается

1. Проверьте наличие Node.js:
   ```bash
   node --version
   ```

2. Проверьте логи:
   ```bash
   cat archive-daemon.log
   ```

### Архивы не создаются

1. Проверьте, запущен ли демон:
   ```bash
   ./start-auto-archive.sh
   ```

2. Проверьте права на запись:
   ```bash
   ls -la ~/Desktop/
   ```

### Остановить зависший процесс

```bash
./stop-auto-archive.sh
# Или вручную:
kill $(cat .archive-daemon.pid)
```

## 💡 Автозапуск при старте системы (опционально)

Для автоматического запуска при загрузке системы добавьте в `~/.zshrc` или `~/.bash_profile`:

```bash
# Автоматическое архивирование проекта
if [ -f "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2/start-auto-archive.sh" ]; then
    cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
    ./start-auto-archive.sh > /dev/null 2>&1
fi
```

## 📊 Статистика

- **Интервал между архивами:** 10 секунд
- **Отслеживаемые файлы:** app.js, index.html, sw.js, manifest.json
- **Расположение архивов:** ~/Desktop/
- **Логи:** archive-daemon.log

---

**Статус:** ✅ Полностью автоматизировано  
**Дата:** 2025-12-01  
**Версия:** 1.0

