# 🔄 Настройка автоматического архивирования при каждом изменении

## ✅ Текущий статус

Автоматическое архивирование настроено! Архивы будут создаваться автоматически при каждом сохранении файлов проекта.

## 📋 Доступные методы

### 1. 🚀 Автоматическое архивирование при сохранении (Рекомендуется)

#### Вариант A: Использование расширения "Run on Save"

1. Установите расширение **"Run on Save"** в Cursor/VS Code:
   - Откройте Extensions (Cmd+Shift+X)
   - Найдите "Run on Save" от emeraldwalk
   - Установите расширение

2. Настройки уже добавлены в `.vscode/settings.json`:
   ```json
   {
       "emeraldwalk.runonsave": {
           "commands": [
               {
                   "match": "\\.(js|html|css|json)$",
                   "cmd": "cd '${workspaceFolder}' && ./auto-archive-on-change.sh auto-save"
               }
           ],
           "shell": true
       }
   }
   ```

3. **Готово!** Теперь при каждом сохранении файлов `.js`, `.html`, `.css`, `.json` будет автоматически создаваться архив.

#### Вариант B: Использование Node.js скрипта

Если расширение "Run on Save" не работает, можно использовать Node.js скрипт:

1. Обновите `.vscode/settings.json`:
   ```json
   {
       "emeraldwalk.runonsave": {
           "commands": [
               {
                   "match": "\\.(js|html|css|json)$",
                   "cmd": "cd '${workspaceFolder}' && node auto-archive-simple.js auto-save"
               }
           ],
           "shell": true
       }
   }
   ```

### 2. 📝 Ручное создание архива

```bash
./quick-archive.sh "описание изменений"
```

или

```bash
node auto-archive-simple.js "описание изменений"
```

### 3. 👀 Мониторинг изменений в реальном времени

Для постоянного мониторинга изменений (если автоматическое сохранение не работает):

```bash
# Установите fswatch (если еще не установлен)
brew install fswatch

# Запустите мониторинг
./watch-and-archive.sh "auto-change"
```

## 📁 Расположение архивов

Все архивы сохраняются в:
```
/Users/alikbidzhiev/Desktop/
```

Формат имени:
```
warehouse-guide-[описание]-[YYYYMMDD-HHMMSS].zip
```

## 📝 Лог архивов

История всех созданных архивов хранится в:
```
archive-log.txt
```

Просмотр лога:
```bash
cat archive-log.txt
# или
tail -f archive-log.txt  # для отслеживания в реальном времени
```

## ⚙️ Настройка

### Изменение описания архива

По умолчанию используется `auto-save`. Чтобы изменить, отредактируйте `.vscode/settings.json`:

```json
{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "\\.(js|html|css|json)$",
                "cmd": "cd '${workspaceFolder}' && ./auto-archive-on-change.sh ваше-описание"
            }
        ]
    }
}
```

### Исключение файлов из архива

Файлы, которые автоматически исключаются:
- `*.DS_Store` (системные файлы macOS)
- `*/.*` (скрытые файлы)
- `*/node_modules/*` (зависимости)
- `*.zip` (старые архивы)

Чтобы изменить список исключений, отредактируйте скрипт `auto-archive-on-change.sh`.

## 🔧 Устранение неполадок

### Проблема: Архивы не создаются автоматически

1. **Проверьте, установлено ли расширение "Run on Save"**
   ```bash
   # В Cursor/VS Code: Extensions → Найдите "Run on Save"
   ```

2. **Проверьте права на выполнение скрипта**
   ```bash
   chmod +x auto-archive-on-change.sh
   ```

3. **Проверьте логи расширения**
   - Откройте Output (Cmd+Shift+U)
   - Выберите "Run on Save" в выпадающем списке
   - Проверьте сообщения об ошибках

### Проблема: Слишком много архивов

Скрипт использует механизм блокировки, чтобы предотвратить создание нескольких архивов одновременно. Если архивы создаются слишком часто:

1. Увеличьте задержку автосохранения в `.vscode/settings.json`:
   ```json
   {
       "files.autoSaveDelay": 5000  // 5 секунд вместо 1
   }
   ```

2. Или отключите автосохранение и сохраняйте вручную (Cmd+S)

### Проблема: Ошибка прав доступа

```bash
chmod +x auto-archive-on-change.sh
chmod +x auto-archive-simple.js
```

### Проблема: Скрипт не найден

Убедитесь, что вы находитесь в правильной директории:
```bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
```

## 💡 Советы

1. **Используйте описательные названия** при создании архивов вручную для важных изменений
2. **Проверяйте лог** регулярно для отслеживания версий
3. **Удаляйте старые архивы** периодически для экономии места
4. **Используйте автоматическое архивирование** для повседневной работы

## 🎯 Проверка работы

1. Откройте любой файл проекта (например, `app.js`)
2. Внесите небольшое изменение
3. Сохраните файл (Cmd+S)
4. Проверьте, что архив создан:
```bash
   ls -lt ~/Desktop/warehouse-guide-*.zip | head -1
```
5. Проверьте лог:
```bash
   tail -1 archive-log.txt
```

---

**Дата настройки:** 2025-01-27  
**Версия:** 2.0  
**Статус:** ✅ Автоматическое архивирование активно
