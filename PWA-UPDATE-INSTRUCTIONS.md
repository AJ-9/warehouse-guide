# 🔄 Инструкция по обновлению PWA приложения

## ⚠️ ВАЖНО: Обновление версии при изменениях

Когда вы вносите изменения в файлы приложения (app.js, index.html, sw.js и т.д.), **ОБЯЗАТЕЛЬНО** обновите версию в файле `sw.js`, иначе установленное PWA не получит обновления!

## Как обновить версию:

### Шаг 1: Откройте файл `sw.js`

Найдите строку:
```javascript
const CACHE_VERSION = 'v2.0.5-20251111';
```

### Шаг 2: Обновите версию

Измените на новую версию, например:
```javascript
const CACHE_VERSION = 'v2.0.6-20251112'; // Увеличьте номер версии и дату
```

**Формат:** `v[версия]-[дата в формате YYYYMMDD]`

Примеры:
- `v2.0.6-20251112` (версия 2.0.6, дата 12 ноября 2025)
- `v2.1.0-20251115` (версия 2.1.0, дата 15 ноября 2025)
- `v3.0.0-20251201` (версия 3.0.0, дата 1 декабря 2025)

### Шаг 3: Сохраните файл

После сохранения изменения автоматически применятся при следующей загрузке.

## Как работает обновление:

1. **Автоматическая проверка:**
   - Каждые 10 секунд приложение проверяет наличие обновлений
   - При возврате фокуса на окно
   - При видимости страницы

2. **Обнаружение обновлений:**
   - Когда Service Worker обнаруживает новую версию, появляется уведомление
   - Пользователь может нажать "Обновить" для применения изменений

3. **Принудительное обновление:**
   - Старые кэши автоматически удаляются
   - Новые файлы загружаются из сети с игнорированием кэша
   - Service Worker активируется немедленно

## Для разработчиков:

### После каждого изменения файлов:

1. ✅ Обновите `CACHE_VERSION` в `sw.js`
2. ✅ Сохраните все файлы
3. ✅ Загрузите обновления на хостинг
4. ✅ Установленное PWA автоматически обнаружит обновление

### Тестирование обновлений:

1. Установите приложение как PWA
2. Внесите изменения в код
3. Обновите версию в `sw.js`
4. Загрузите на хостинг
5. Откройте установленное PWA
6. Должно появиться уведомление об обновлении

## Устранение проблем:

### Проблема: Обновления не применяются

**Решение:**
1. Проверьте, что версия в `sw.js` обновлена
2. Очистите кэш браузера вручную
3. Переустановите PWA (удалите и установите заново)

### Проблема: Уведомление не появляется

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что Service Worker зарегистрирован
3. Проверьте, что версия действительно изменилась

### Проблема: Старая версия все еще показывается

**Решение:**
1. Нажмите кнопку "Обновить" в уведомлении
2. Или перезагрузите страницу принудительно (Ctrl+Shift+R или Cmd+Shift+R)
3. Или закройте и откройте приложение заново

## Текущая версия:

Версия в `sw.js`: `v2.0.5-20251111`

---

**Помните:** Без обновления версии в `sw.js` установленное PWA не получит обновления!

