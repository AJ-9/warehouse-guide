# 🌍 Настройка многоязычности (i18n)

## ✅ Что уже реализовано

### 1. **UI элементы переведены**
- ✅ Заголовки и кнопки
- ✅ Форма обратной связи
- ✅ Сообщения об ошибках и успехе
- ✅ Переключатель языка в шапке

### 2. **Поддерживаемые языки**
- 🇷🇺 **Русский** (ru) - по умолчанию
- 🇬🇧 **English** (en)
- 🇨🇳 **中文** (zh) - Китайский

### 3. **Функциональность**
- ✅ Сохранение выбранного языка в localStorage
- ✅ Автоматическое применение языка при загрузке
- ✅ Переключение языка без перезагрузки страницы

## 📝 Что нужно сделать для полной многоязычности

### **ВАЖНО: Перевод контента (warehouseData)**

Сейчас переведены только **UI элементы** (кнопки, формы, сообщения). 

**Контент справочника** (главы, разделы, описания) пока остается только на русском языке.

### Как добавить переводы контента:

#### Вариант 1: Реорганизация структуры данных (рекомендуется)

Измените структуру `warehouseData` в `app.js`:

```javascript
const warehouseData = {
    ru: {
        chapters: [...],
        content: {...}
    },
    en: {
        chapters: [...],
        content: {...}
    },
    zh: {
        chapters: [...],
        content: {...}
    }
};
```

Затем обновите функции `renderChapters()`, `showContentDirect()`, и другие, чтобы они использовали:
```javascript
warehouseData[currentLanguage].chapters
warehouseData[currentLanguage].content
```

#### Вариант 2: Добавление переводов в существующую структуру

Добавьте поля переводов в каждую запись:

```javascript
{
    id: 'chapter1',
    title: {
        ru: 'Процесс приемки',
        en: 'Receiving Process',
        zh: '接收流程'
    },
    subchapters: [
        {
            id: '1.1',
            title: {
                ru: 'Заезд ТС на площадку',
                en: 'Vehicle Arrival',
                zh: '车辆到达'
            }
        }
    ]
}
```

Затем обновите функции для использования:
```javascript
chapter.title[currentLanguage] || chapter.title.ru
```

## 🎯 Текущий статус

| Компонент | Русский | English | 中文 |
|-----------|---------|---------|------|
| UI элементы | ✅ | ✅ | ✅ |
| Контент справочника | ✅ | ❌ | ❌ |
| Форма обратной связи | ✅ | ✅ | ✅ |
| Сообщения | ✅ | ✅ | ✅ |

## 📊 Объем работы для перевода контента

- **Глав:** 6
- **Подглав:** ~40
- **Разделов контента:** ~36
- **Общий объем текста:** ~3000+ строк

**Оценка времени:**
- Перевод на английский: ~2-3 часа
- Перевод на китайский: ~3-4 часа
- Тестирование: ~1 час

## 🔧 Технические детали

### Функции для работы с переводами:

```javascript
// Получить перевод
t('appTitle') // Вернет перевод для текущего языка

// Переключить язык
changeLanguage('en') // Переключить на английский

// Текущий язык
currentLanguage // 'ru', 'en', или 'zh'
```

### Добавление новых переводов:

1. Откройте `app.js`
2. Найдите объект `translations`
3. Добавьте новый ключ во все языки (ru, en, zh)
4. Используйте `t('yourKey')` в коде

## 🚀 Следующие шаги

1. **Решить структуру данных** - выбрать вариант 1 или 2
2. **Перевести контент** - добавить переводы для всех глав и разделов
3. **Обновить функции** - изменить функции для использования переводов
4. **Протестировать** - проверить работу на всех языках

## 💡 Рекомендации

- Начните с перевода одной главы для тестирования
- Используйте профессиональный переводчик для китайского языка
- Проверьте корректность отображения китайских иероглифов
- Убедитесь, что все ссылки и изображения работают на всех языках

---

**Статус:** Базовая многоязычность реализована ✅  
**Следующий этап:** Перевод контента справочника

