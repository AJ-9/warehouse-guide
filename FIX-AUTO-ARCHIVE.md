# 🔧 Исправление автоматического архивирования

## ❌ Проблема: Архивы не создаются автоматически

## ✅ Решения:

### Решение 1: Установите расширение "Run on Save" (Рекомендуется)

1. **Откройте Cursor/VS Code**
2. **Нажмите `Cmd+Shift+X`** (или `Ctrl+Shift+X`)
3. **Найдите расширение "Run on Save"** от **emeraldwalk**
4. **Установите расширение**
5. **Перезагрузите редактор** (`Cmd+Q` и откройте снова)

**Проверка:**
- Откройте Output (`Cmd+Shift+U`)
- Выберите "Run on Save" в выпадающем списке
- Сохраните файл (`Cmd+S`)
- Должны появиться сообщения о выполнении команды

### Решение 2: Используйте file watcher (fswatch)

Если расширение не работает, используйте встроенный мониторинг:

```bash
# Установите fswatch (если еще не установлен)
brew install fswatch

# Запустите мониторинг
./watch-and-archive.sh "auto-change"
```

Или используйте новый скрипт:

```bash
# Запустите настройку
./setup-auto-archive.sh

# Затем запустите мониторинг
./watch-files.sh
```

### Решение 3: Используйте Git hooks (если используете Git)

Создайте файл `.git/hooks/post-commit`:

```bash
#!/bin/bash
cd "/Users/alikbidzhiev/Desktop/warehouse-guide-corrected 2"
./auto-archive-on-change.sh "git-commit"
```

Сделайте его исполняемым:
```bash
chmod +x .git/hooks/post-commit
```

### Решение 4: Ручное архивирование

Если автоматическое не работает, создавайте архив вручную:

```bash
./quick-archive.sh "описание изменений"
```

## 🔍 Диагностика проблем

### Проверка 1: Работает ли скрипт?

```bash
./auto-archive-on-change.sh test
```

Если архив создается - скрипт работает правильно.

### Проверка 2: Установлено ли расширение?

1. Откройте Extensions (`Cmd+Shift+X`)
2. Найдите "Run on Save"
3. Проверьте, что оно установлено и включено

### Проверка 3: Правильные ли настройки?

Проверьте файл `.vscode/settings.json`:

```json
{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "\\.(js|html|css|json)$",
                "cmd": "cd '${workspaceFolder}' && ./auto-archive-on-change.sh auto-save"
            }
        ],
        "shell": true
    }
}
```

### Проверка 4: Права на выполнение

```bash
chmod +x auto-archive-on-change.sh
chmod +x quick-archive.sh
```

## 💡 Рекомендации

1. **Используйте расширение "Run on Save"** - это самый надежный способ
2. **Проверяйте логи** в Output панели Cursor/VS Code
3. **Используйте ручное архивирование** для важных изменений
4. **Запускайте file watcher** в отдельном терминале для постоянного мониторинга

## 📝 Быстрая проверка

Выполните эту команду для проверки всех компонентов:

```bash
# Проверка скрипта
./auto-archive-on-change.sh test-check && echo "✅ Скрипт работает"

# Проверка прав
ls -la auto-archive-on-change.sh | grep -q "x" && echo "✅ Права установлены"

# Проверка настроек
test -f .vscode/settings.json && echo "✅ Настройки существуют"
```

---

**Дата:** 2025-12-01  
**Статус:** Скрипт работает, требуется установка расширения или использование file watcher

