# 🎯 Контекст проекта для нового агента

## 📋 Общая информация

**Название проекта:** Библия Кладовщика / Справочник СКЛАД  
**Тип:** Progressive Web App (PWA) - веб-приложение со справочной информацией  
**Язык:** Русский  
**Технологии:** HTML5, CSS3, Vanilla JavaScript, Service Worker (PWA)

## 🏗️ Архитектура проекта

### Основные файлы:

1. **`index.html`** (1114 строк)
   - Главная страница приложения
   - Содержит весь HTML разметку и встроенные CSS стили
   - Структура: header, search, navigation, content area
   - Адаптивный дизайн с фоновым изображением

2. **`app.js`** (2642 строки)
   - Основная логика приложения
   - Содержит объект `warehouseData` с данными справочника:
     - `chapters` - массив глав (6 глав)
     - `content` - объект с содержимым разделов
   - Функции поиска, навигации, отображения контента
   - Обработка изображений и модальных окон

3. **`sw.js`** (164 строки)
   - Service Worker для PWA функциональности
   - Кэширование ресурсов для офлайн работы
   - Версионирование кэша

4. **`manifest.json`** (28 строк)
   - PWA манифест
   - Настройки приложения, иконки, темы

5. **`images/`** - папка с изображениями
   - ~40 изображений из оригинального PDF
   - Используются в разделах справочника

## 📚 Структура данных

### Главы справочника:

1. **Глава 1: Процесс приемки** (9 подразделов)
   - Заезд ТС, проверка документов, выгрузка, акты

2. **Глава 2: Хранение материала** (5 подразделов)
   - Правила хранения, коды, перемещение ТМЦ

3. **Глава 3: Выдача материала в монтаж** (5 подразделов)
   - Выдача по заявкам, частичная выдача, акты

4. **Глава 4: Документация** (9 подразделов)
   - Правила оформления ТН, CMR, ведомостей, пропусков

5. **Глава 5: Информационные группы** (4 подраздела)
   - Правила работы с отделом учета, полезные ссылки

6. **Глава 6: Общие хозяйственные вопросы** (4 подраздела)
   - Заявки, время труда, отпуска, документы

## 🔧 Как работать с проектом

### Редактирование контента:

1. **Добавление/изменение раздела:**
   - Откройте `app.js`
   - Найдите объект `warehouseData`
   - Добавьте/измените запись в `chapters` (для структуры)
   - Добавьте/измените запись в `content` (для содержимого)

2. **Изменение стилей:**
   - Откройте `index.html`
   - Найдите тег `<style>` (примерно строка 12)
   - Измените CSS стили

3. **Добавление изображений:**
   - Поместите изображение в папку `images/`
   - В `app.js` в объекте `content` используйте путь: `images/имя_файла.png`

### Структура объекта content:

```javascript
'1.1': {
    title: 'Название раздела',
    content: `
        <h3>Подзаголовок</h3>
        <p>Текст</p>
        <img src="images/image.png" alt="Описание">
    `,
    images: ['images/image.png'] // опционально
}
```

## 🚀 Запуск проекта

### Локально:
```bash
# Python
python -m http.server 8000

# Node.js
npx serve .

# PHP
php -S localhost:8000
```

Затем откройте `http://localhost:8000`

### Развертывание:
- GitHub Pages
- Netlify
- Vercel
- Любой статический хостинг

## 📝 Важные особенности

1. **PWA функциональность:**
   - Приложение можно установить как нативное
   - Работает офлайн после первой загрузки
   - Service Worker кэширует ресурсы

2. **Поиск:**
   - Полнотекстовый поиск по всем разделам
   - Подсветка найденных результатов
   - Быстрая навигация

3. **Навигация:**
   - Древовидная структура глав и подразделов
   - Хлебные крошки для навигации
   - История просмотра

4. **Адаптивность:**
   - Работает на всех устройствах
   - Мобильная версия оптимизирована

## 🔄 История изменений

См. файл `CHANGES.md` для детальной информации об исправлениях и обновлениях.

## 📦 Архивация

### Автоматическое архивирование

Проект настроен для **автоматического создания архивов после каждого изменения**:

1. **`watch-and-archive.sh`** - мониторинг изменений в реальном времени
   ```bash
   ./watch-and-archive.sh "auto-change"
   ```
   - Отслеживает все изменения файлов
   - Создает архив каждые 30 секунд (если были изменения)
   - Работает в фоновом режиме

2. **`auto-archive-on-save.js`** - Node.js версия для интеграции с редакторами
   ```bash
   node auto-archive-on-save.js "auto-save"
   ```

3. **Ручное создание архивов:**
   - `quick-archive.sh` - быстрое создание архива
   - `create-archive.sh` - подробное создание с информацией
   - `auto-archive.js` - Node.js версия с логом

### Настройка автоматического архивирования

См. файл `AUTO-ARCHIVE-SETUP.md` для детальных инструкций по настройке:
- Интеграция с Cursor/VS Code
- Git hooks
- Фоновый мониторинг

Архивы сохраняются в `/Users/alikbidzhiev/Desktop/` с именем:
`warehouse-guide-[описание]-[дата-время].zip`

История архивов ведется в `archive-log.txt`

## ⚠️ Важные замечания

1. **Не используйте Node.js зависимости** - проект чистый HTML/CSS/JS
2. **Service Worker требует HTTPS** (или localhost) для работы
3. **Изображения должны быть оптимизированы** для быстрой загрузки
4. **Все пути к файлам относительные** - не используйте абсолютные пути

## 🎨 Стилистика кода

- JavaScript: ES6+, без фреймворков
- CSS: Встроенный в HTML, современные свойства
- HTML: Семантическая разметка, доступность

## 📞 Контакты и ссылки

В проекте есть ссылки на:
- Google Sheets для диспетчерского файла ТС
- Информационные группы (указаны в разделе 5)

---

**Дата создания контекста:** 2025-11-11  
**Версия проекта:** 2.0 (полное соответствие оригинальному PDF)

